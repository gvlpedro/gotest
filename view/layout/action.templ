package layout

templ Action(status string) {
	<div id="action">
		<button hx-post="/" hx-target="#action" hx-swap="outerHTML">Send</button>
		<p>
			Status: 
			if status == "sent" {
				Sent
			}
			if status == "unsent" {
				Unsent
			}
		</p>
	</div>
}
